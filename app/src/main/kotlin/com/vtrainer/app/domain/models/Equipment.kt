package com.vtrainer.app.domain.models

/**
 * Enum representing equipment required for exercises.
 */
enum class Equipment {
    BARBELL,
    DUMBBELL,
    MACHINE,
    CABLE,
    BODYWEIGHT,
    BENCH,
    PULL_UP_BAR
}
